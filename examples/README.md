## Example: Candy Calorie Counter

The [candy_calorie_counter](candy_calorie_counter) script uses a custom YOLO model trained to identify popular types of candy (Skittles, Snickers, and so on). When candy is in front of the camera, it looks up calories and grams of sugar per piece from a table keyed by class name and reports totals for everything in view.
